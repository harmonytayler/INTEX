<!-- Form to edit scheduled events -->

<!DOCTYPE html>
<html>
<head>
    <title>Edit Scheduled Event</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1 style="text-align: center;">Edit Scheduled Event</h1>
    
    <!-- Route to post changes -->
    <form action="/editScheduled/<%= scheduledEvents.event_id %>" method="POST">

        <!-- Description: Character Varying -->
        <label for="description">Description</label>
        <input type="text" id="description" name="description" value="<%= scheduledEvents.description %>" required>
        <br><br>

        <!-- Anticipated_Participants: Integer -->
        <label for="anticipated_participants">Anticipated Participants</label>
        <input type="number" id="anticipated_participants" name="anticipated_participants" value="<%= scheduledEvents.anticipated_participants %>">
        <br><br>

        <!-- Under_Ten: Integer -->
        <label for="under_ten">Under Ten</label>
        <input type="number" id="under_ten" name="under_ten" value="<%= scheduledEvents.under_ten %>">
        <br><br>

        <!-- Possible_Date: Datetime -->
        <label for="possible_date">Possible Date</label>
        <input type="date" id="possible_date" name="possible_date" value="<%= scheduledEvents.possible_date.toISOString().split('T')[0] %>">
        <br><br>

        <!-- Type: Character Varying/ Radio -->
        <label>Event Type</label>
        <label>
            <input type="radio" name="type" value="Sewing" <%= scheduledEvents.type === 'Sewing' ? 'checked' : '' %>> Sewing
        </label>
        <label>
            <input type="radio" name="type" value="Non-sewing"<%= scheduledEvents.type === 'Non-sewing' ? 'checked' : '' %>> Non-sewing
        </label>
        <label>
            <input type="radio" name="type" value="Both" <%= scheduledEvents.type === 'Non-sewing' ? 'checked' : '' %>> Both
        </label>
        <br><br>

        <!-- Address: Character Varying -->
        <label for="address">Address</label>
        <input type="text" id="address" name="address" value="<%= scheduledEvents.address %>">
        <br><br>

        <!-- Start_Time: Time -->
        <label for="start_time">Start Time</label>
        <input type="time" id="start_time" name="start_time" value="<%= scheduledEvents.start_time %>">
        <br><br>

        <!-- Duration: Character Varying -->
        <label for="duration">Duration</label>
        <input type="text" id="duration" name="duration" value="<%= scheduledEvents.duration %>">
        <br><br>

        <!-- Contact_Name: Character Varying -->
        <label for="contact_name">Contact Name</label>
        <input type="text" id="contact_name" name="contact_name" value="<%= scheduledEvents.contact_name %>">
        <br><br>

        <!-- Contact_Phone: Character Varying -->
        <label for="contact_phone">Contact Phone</label>
        <input type="text" id="contact_phone" name="contact_phone" value="<%= scheduledEvents.contact_phone %>">
        <br><br>

        <!-- Contact_Email: Character Varying -->
        <label for="contact_email">Contact Email</label>
        <input type="text" id="contact_email" name="contact_email" value="<%= scheduledEvents.contact_email %>">
        <br><br>

        <!-- Machines: Integer -->
        <label for="machines">Machines</label>
        <input type="number" id="machines" name="machines" value="<%= scheduledEvents.machines %>">
        <br><br>

        <!-- Sewers: Integer -->
        <label for="sewers">Sewers</label>
        <input type="number" id="sewers" name="sewers" value="<%= scheduledEvents.sewers %>">
        <br><br>

        <!-- Story: Character Varying/ Radio -->
        <label>Did Jen share her story?</label>
        <label>
            <input type="radio" name="story" value="Yes, long" <%= scheduledEvents.story === 'Yes, long' ? 'checked' : '' %>> Yes, Long
        </label>
        <label>
            <input type="radio" name="story" value="Yes, short" <%= scheduledEvents.story === 'Yes, short' ? 'checked' : '' %>> Yes, Short
        </label>
        <label>
            <input type="radio" name="story" value="No" <%= scheduledEvents.story === 'No' ? 'checked' : '' %>> No
        </label>
        <br><br>

        <!-- Members_Needed: Integer -->
        <label for="members_needed">Members Needed</label>
        <input type="number" id="members_needed" name="members_needed" value="<%= scheduledEvents.members_needed %>">
        <br><br>

        <!-- Status Character Varying / Radio-->
        <label>Status</label>
        <label>
            <input type="radio" name="status" value="Pending" <%= scheduledEvents.status === 'Pending' ? 'checked' : '' %>> Pending
        </label>
        <label>
            <input type="radio" name="status" value="Approved" <%= scheduledEvents.status === 'Approved' ? 'checked' : '' %>> Approved
        </label>
        <label>
            <input type="radio" name="status" value="Denied" <%= scheduledEvents.status === 'No' ? 'Denied' : '' %>> Denied
        </label>
        <label>
            <input type="radio" name="status" value="Completed" <%= scheduledEvents.status === 'No' ? 'Completed' : '' %>> Completed
        </label>
        <br><br>

        <!-- Notes: Character Varying -->
        <label for="notes">Notes</label>
        <input type="text" id="notes" name="notes" value="<%= scheduledEvents.notes %>">
        <br><br>

        <!-- Submit Button -->
        <button type="submit">Edit</button>
    </form>
    <form action="/scheduledEvents" method="GET" style="display: flex; justify-content: center; margin: 20px 0;">
        <button type="submit">Back</button>
    </form>
</body>
</html>
